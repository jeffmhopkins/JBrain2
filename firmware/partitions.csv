# Partition table for the room endpoint (Waveshare ESP32-S3-Touch-AMOLED-1.8, 16 MB flash).
#
# THIS TABLE IS EFFECTIVELY PERMANENT. OTA rewrites an app slot and nothing else, so changing
# the layout means a USB reflash — and both units are going to the twins, where a cable is a
# physical trip. So it is laid out for what the endpoint will eventually be, not for what the
# first firmware needs, and the space is reserved now while reserving it is free.
#
# Comments only ever get their own line here: anything after the size column is parsed as the
# Flags field, which is how the first version of this file failed to build.
#
#   nvs       24K    wifi creds, api url, device token, box CA root
#   otadata    8K    which slot boots, and whether it is trusted yet
#   factory   3.5M   the frozen recovery app. Never OTA'd.
#   ota_0/1   3.5M   each
#   model     3.5M   the ESP-SR wake word and command models
#   storage   1.875M assets. Ends exactly at 16M.
#
# RESIZED 2026-09-21, and this is the one change that needs a USB flash rather than an OTA.
# Linking ESP-SR took the app from 1.16M to 3.05M, which does not fit a 1.5M `factory` — and
# `factory` is where a USB flash writes, so the image would not have gone on at all. Three
# EQUAL 3.5M slots: the recovery app and the two OTA slots all hold the same image, so any
# asymmetry between them is a slot that cannot take an update the others can.
#
# `model` and `storage` keep their exact offsets (0xAA0000, 0xE20000) because the three app
# slots gave up 3M between them and factory took it. That was worth engineering for: every
# constant that moves is another place a panel can be bricked from, and the box's flasher
# hardcodes the model offset.
#
# Headroom is 0.45M per slot at 3.05M. If that runs out, the next move is dropping `factory`
# for two 5.5M slots — rung 2 of ENDPOINT_RECOVERY.md's ladder is already a copy of the same
# image, so it costs less than it looks.
#
# Name,     Type, SubType,  Offset,    Size
nvs,        data, nvs,      0x9000,    0x6000
otadata,    data, ota,      0xF000,    0x2000
phy_init,   data, phy,      0x11000,   0x1000
factory,    app,  factory,  0x20000,   0x380000
ota_0,      app,  ota_0,    0x3A0000,  0x380000
ota_1,      app,  ota_1,    0x720000,  0x380000
model,      data, spiffs,   0xAA0000,  0x380000
storage,    data, spiffs,   0xE20000,  0x1E0000
