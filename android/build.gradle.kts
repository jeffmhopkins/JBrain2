// The JBrain360 Android app (Phase 7 M5): the forked tracker's host. M5a is the
// WebView shell; native Keystore + /session/mint (M5b) and pairing (M5c) follow.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
