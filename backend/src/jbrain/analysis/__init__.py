"""Note analysis: the graph a note's reading lands — facts, entities, mentions,
temporal tokens, review items (docs/reference/ANALYSIS.md).

The reading is the note conversation's (`converse.py` + `agent/graphwritetools.py`); the
deterministic EMR importer brings its own. The `note.extract` call that used to produce it
was deleted in R4 of docs/plans/AGENT_INGEST_REWRITE.md."""
