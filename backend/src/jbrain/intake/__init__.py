"""Guided-intake share links: the non-owner capability layer (Phase 7, W1).

The owner mints a link that gives a stranger a scoped chat interviewer; the
recipient's confirmed submission surfaces owner-side as an approvable Proposal.
This package owns the data foundation and the mint/redeem/revoke flows; the
persona (W2), the chat endpoint + capture (W3), and the Proposals (W4) build on it.
"""
